<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eldercare.core.mapper.PermissionMapper">
    <select id="selectApiResourcesByUserId" resultType="com.eldercare.core.mapper.PermissionMapper$ApiResource">
        SELECT DISTINCT r.path, r.method FROM user_role ur
        JOIN role_permission rp ON rp.role_id=ur.role_id
        JOIN permission_resource pr ON pr.permission_id=rp.permission_id
        JOIN resource r ON r.id=pr.resource_id
        WHERE ur.user_id=#{userId} AND r.type='API' AND r.path IS NOT NULL
    </select>
</mapper>
